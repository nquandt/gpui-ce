[package]
name = "ios_text_input"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
name = "ios_text_input"
crate-type = ["staticlib"]

[dependencies]
gpui = { path = "../../crates/gpui/", package = "gpui-ce" }
gpui_ce_elements = { path = "../../crates/gpui_elements/" }
gpui_mobile = { path = "../../crates/gpui_mobile/", default-features = false, features = [
  "video_player",
  "camera",
] }
log.workspace = true
